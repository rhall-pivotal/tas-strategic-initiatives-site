---
platform: linux

image: docker:///pivotalcf/releng-ci

inputs:
  - name: p-runtime
  - name: version
  - name: upstream-release-cf-autoscaling
  - name: upstream-release-notifications
  - name: upstream-release-notifications-ui

params:
  RELEASE_NAMES:
  EXTERNAL_RELEASE:
  GITHUB_ACCESS_TOKEN:
  GITHUB_USER_NAME:
  GITHUB_RELEASE_ORGS:
  GITHUB_RELEASE_REPOS:
  USE_ERT_DUMMY:
  DERIVE_METADATA: true

run:
  path: p-runtime/ci/scripts/build-pivotal
