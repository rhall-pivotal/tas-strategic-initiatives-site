---
platform: linux

image: docker:///cloudfoundry/releng-ci

inputs:
  - name: p-runtime
  - name: version
  - name: upstream-release

params:
  GITHUB_USER_NAME:
  GITHUB_ACCESS_TOKEN:
  GITHUB_RELEASE_ORG:
  GITHUB_RELEASE_REPO:
  RELEASE_NAME:
  EXTERNAL_RELEASE:
  USE_ERT_DUMMY:
  DERIVE_METADATA: true

run:
  path: p-runtime/ci/scripts/build-pivotal
