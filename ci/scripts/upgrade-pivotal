#!/bin/bash

set -ex

export RELENG_ENV=`cat environment/name`
export PIVOTAL_PATH=$PWD/ert-product/ert.pivotal

pushd p-runtime
  ./ci/scripts/helpers/bundle-gems.sh

  xvfb-run -a rake opsmgr:product:upload_upgrade[$RELENG_ENV,$OPSMAN_VERSION,$PIVOTAL_PATH,cf]
popd
