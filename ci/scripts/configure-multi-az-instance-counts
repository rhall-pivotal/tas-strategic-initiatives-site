#!/bin/bash

set -ex

export RELENG_ENV=`cat environment/name`

pushd p-runtime
  ./ci/scripts/helpers/bundle-gems.sh

  xvfb-run -a bundle exec rake ert:configure-multi-az-instance-counts[$RELENG_ENV,$ERT_VERSION,$OPSMAN_VERSION]
popd
