from_version: 1.3.0.0
rules:
  - type: update
    selector: "product_version"
    to: "1.4.0.0$PRERELEASE_VERSION$"
  - type: update
    selector: "jobs.[identifier=compilation].resources.[identifier=ephemeral_disk].value"
    to: 20480
