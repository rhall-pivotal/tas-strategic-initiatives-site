from_version: 1.4.1.0
rules:
  - type: update
    selector: "product_version"
    to: "1.5.0.0$PRERELEASE_VERSION$"

  - type: update
    selector: "jobs.[identifier=mysql_proxy].resources.[identifier=ephemeral_disk].value"
    to: 4_096
