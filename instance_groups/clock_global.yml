---
name: clock_global
label: Clock Global
resource_label: Clock Global
description: Schedules asynchronous tasks for cloud controller
templates:
{{if eq tile "ert"}}
- $( job "bpm" )
- $( job "cloud_controller_clock" )
- $( job "rotate_cc_database_key" )
- $( job "cc_deployment_updater" )
- $( job "loggregator_agent" )
- $( job "loggr-syslog-agent" )
- $( job "metrics-discovery-registrar" )
- $( job "metrics-agent" )
- $( job "loggr-forwarder-agent" )
- $( job "loggr-system-metric-scraper" )
- $( job "leadership-election" )
- $( job "loggr-syslog-binding-cache" )
- $( job "prom_scraper" )
- $( job "syslog_forwarder" )
- $( job "cf-cli-6-linux" )
- $( job "smoke_tests" )
- $( job "push-usage-service" )
- $( job "nodejs-buildpack" )
- $( job "ruby-buildpack" )
- $( job "push-apps-manager" )
- $( job "deploy-notifications" )
- $( job "go-buildpack" )
- $( job "deploy-notifications-ui" )
- $( job "binary-buildpack" )
- $( job "java-buildpack" )
- $( job "deploy-autoscaler" )
- $( job "nfsbrokerpush" )
- $( job "smbbrokerpush" )
- $( job "bootstrap" )
- $( job "test-autoscaling" )
- $( job "metric_registrar_orchestrator" )
- $( job "metric_registrar_smoke_test" )
{{else}}
- $( job "placeholder" )
{{end}}

resource_definitions:
- name: ram
  type: integer
  label: RAM
  configurable: true
  default: 1024
  constraints:
    min: 1024
- name: ephemeral_disk
  type: integer
  label: Ephemeral Disk
  configurable: true
  default: 20480
  constraints:
    min: 20480
- name: persistent_disk
  type: integer
  label: Persistent Disk
  configurable: false
  default: 0
- name: cpu
  type: integer
  label: CPU
  configurable: true
  default: 1
  constraints:
    min: 1
static_ip: 0
dynamic_ip: 1
max_in_flight: 1
single_az_only: false
instance_definition:
  name: instances
  type: integer
  label: Instances
  configurable: $( variable "clock_global_configurable_state" )
  default: $( variable "clock_global_default_instances" )
  constraints:
    min: $( variable "clock_global_min_instances" )
