---
name: smoke_tests
label: Smoke Tests
description: Smoke Tests
markdown: |
  To configure this pane, see the
  [Configure Smoke Tests](https://docs.pivotal.io/pivotalcf/2-7/customizing/configure-pas.html#config-smoke-test)
  section of the _Configuring PAS_ topic in the PCF documentation.

property_inputs:
  - reference: .properties.smoke_tests
    label: |
      Choose where to deploy apps when running smoke tests:
    selector_property_inputs:
    - reference: .properties.smoke_tests.on_demand
      label: |
        Temporary space within the system organization

    - reference: .properties.smoke_tests.specified
      label: |
        Specified organization and space
      property_inputs:
        - reference: .properties.smoke_tests.specified.org_name
          label: Organization
          description: |
            The PAS organization to use when running tests.

        - reference: .properties.smoke_tests.specified.space_name
          label: Space
          description: The PAS space to use when running tests.

        - reference: .properties.smoke_tests.specified.apps_domain
          label: Domain
          description: The domain to which the organization is routed.
