---
name: advanced_features
label: Advanced Features
description: Advanced Features
markdown: |
  For help configuring these fields, see
  [Configure Advanced Features](https://docs.pivotal.io/platform/application-service-windows/2-10/installing.html#config-advanced-features)
  in _Installing and Configuring TAS for VMs [Windows]_ in the VMware documentation.

property_inputs:
  - reference: .windows_diego_cell.executor_memory_capacity
    label: Diego Cell memory capacity
    description: Enter value in MB.

  - reference: .windows_diego_cell.executor_disk_capacity
    label: Diego Cell disk capacity
    description: Enter value in MB.

  - reference: .properties.route_integrity
    label: TLS connections from the Gorouter to apps (beta)
    description: |
      If you have mutual TLS app identity verification enabled, app containers
      accept incoming communication only from the Router. This disables TCP routing.
    selector_property_inputs:
    - reference: .properties.route_integrity.disable
      label: Disable route integrity and mutual TLS
    - reference: .properties.route_integrity.tls_verify
      label: The Gorouter uses TLS to verify app identity
    - reference: .properties.route_integrity.mutual_tls_verify
      label: The Gorouter and apps use mutual TLS to verify each other's identity
