---
name: advanced_features
label: Advanced Features
description: Advanced Features
markdown: |
  For help configuring these fields, see the
  [Advanced Features](https://docs.pivotal.io/pivotalcf/2-10/customizing/installing-pcf-is.html#advanced_features)
  section of the _Installing Isolation
  Segment_ topic in the product documentation.

property_inputs:
- reference: .isolated_diego_cell.executor_memory_capacity
  label: Diego Cell memory capacity
  description: Enter value in MB.

- reference: .isolated_diego_cell.executor_disk_capacity
  label: Diego Cell disk capacity
  description: Enter value in MB.

- reference: .properties.app_graceful_shutdown_period_in_seconds
  label: App graceful shutdown period
  description: |
      The amount of time to wait for an app instance to exit once
      it has been signalled to gracefully shut down.
      Enter value in seconds. The default value is 10.
  placeholder: 10

- reference: .properties.enable_smb_volume_driver
  label: Enable SMB volume services
  description: |
    Allow app developers to bind existing SMB shares to apps
    for shared file access.
