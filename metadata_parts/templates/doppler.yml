---
name: doppler
release: loggregator
manifest: |
  loggregator:
    disable_syslog_drains: true
    etcd:
      machines: []
    tls:
      doppler:
        cert: (( doppler_tls_cert.cert_pem ))
        key: (( doppler_tls_cert.private_key_pem ))
      ca_cert: (( $ops_manager.ca_certificate ))
      cipher_suites: (( .properties.doppler_ssl_ciphers.value ))
  doppler_endpoint:
    shared_secret: (( .doppler.shared_secret_credentials.password ))
  doppler:
    disable_announce: true
    message_drain_buffer_size: (( message_drain_buffer_size.value ))
