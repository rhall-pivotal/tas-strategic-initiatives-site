---
name: rejoin-unsafe
release: cf-mysql
manifest: |
  cf_mysql:
    mysql:
      galera_healthcheck:
        endpoint_username: ((( bootstrap-db-credentials.username )))
        endpoint_password: ((( bootstrap-db-credentials.password )))
