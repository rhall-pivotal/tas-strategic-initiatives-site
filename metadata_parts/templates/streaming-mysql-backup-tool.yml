---
name: streaming-mysql-backup-tool
release: mysql-backup
manifest: |
  cf_mysql:
    mysql:
      admin_password: (( mysql_admin_credentials.password ))
  cf-mysql-backup:
    endpoint_credentials:
      username: ((( mysql-backup-server-credentials.username )))
      password: ((( mysql-backup-server-credentials.password )))
