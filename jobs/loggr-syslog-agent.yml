---
name: loggr-syslog-agent
release: loggregator-agent
manifest: |
  enabled: true
  port: 3460
  tls:
    ca_cert: (( $ops_manager.ca_certificate ))
    cert: (( .doppler.metron_tls_cert.cert_pem ))
    key: (( .doppler.metron_tls_cert.private_key_pem ))
  cache:
    tls:
      ca_cert: (( $ops_manager.ca_certificate ))
      cert: (( .syslog_scheduler.syslog_agent_api_tls.cert_pem ))
      key: (( .syslog_scheduler.syslog_agent_api_tls.private_key_pem ))
      cn: binding-cache