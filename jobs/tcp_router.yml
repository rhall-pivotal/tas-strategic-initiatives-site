---
name: tcp_router
release: routing
manifest: |
  uaa:
    tls_port: 8443
    ca_cert: (( $ops_manager.ca_certificate ))
  tcp_router:
    oauth_secret: (( .uaa.tcp_router_credentials.password ))
    log_level: debug
