---
name: smoke_tests_isolation
release: smoke-tests
manifest: |
  smoke_tests: (( .properties.smoke_tests_isolation.selected_option.parsed_manifest(smoke_tests_isolation) ))
  bpm:
    enabled: true
