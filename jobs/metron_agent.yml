name: metron_agent
release: loggregator
manifest: |
  loggregator:
    tls:
      ca_cert: (( $ops_manager.ca_certificate ))
      metron:
        cert: (( .properties.metron_tls_cert.cert_pem ))
        key: (( .properties.metron_tls_cert.private_key_pem ))
  metron_agent:
    tags:
      placement_tag: (( .isolated_diego_cell.placement_tag.value ))
      product: $( variable "label" )
      product_version: $( variable "build-version" )
      system_domain: "(( ..cf.cloud_controller.system_domain.value ))"
